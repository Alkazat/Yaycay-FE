import { BrandLoading } from "@/components/shell/BrandLoading";

/** Route-level loading for the free-demo entry. */
export default function Loading() {
  return <BrandLoading label="Warming up your free day…" />;
}
